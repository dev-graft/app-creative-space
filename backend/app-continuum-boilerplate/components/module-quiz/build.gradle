dependencies {
    implementation(project(':features:support-advice'))
    implementation(project(':features:support-event'))
    implementation(project(':components:quiz'))
    testImplementation(project(':components:quiz'))
}
